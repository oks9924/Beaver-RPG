# 검증 기록 (단계 0·1)

실행 환경: Linux 컨테이너, Godot 4.4.1 stable (편집기 바이너리 headless 실행), 서버·클라이언트 모두 같은 컴퓨터의 별도 프로세스. 실제 Windows PC 와 인터넷 접속은 **미검증**.

## 단계 3·4 자동 검증 (모두 통과, 2026-09-20, v0.2.0)
| 명령 | 내용 | 결과 |
|---|---|---|
| `-- --tool=lint_all` | 50개 스크립트 파싱·컴파일 (미선언 식별자도 실패로 셈) | 0 실패 |
| `-- --tool=check_assets` | 매니페스트 399항목 (v4 연결 후, 임시 0) | 문제 0 |
| `-- --tool=check_routes` | 시드 100개 × 3지역 18층: 재현성, 노드·방·자산 유효성, 지역 보스 3 | 문제 0 |
| `-- --tool=run_tests` | 단위 552개: 스냅샷 코덱 왕복, v3·v4 에셋 규격, 로그라이크 규칙, 5직업 스킬·자원·상한, 변형·진화·특성·시너지·건설 종류, 지역·적 9종·호위·정예·위험 구역·상태 효과, 퀘스트·결말·인연·마을 상한·비밀·유물 효과, 난이도·튜토리얼·핑·중단/이어하기 | 552/552 |
| `-- --tool=test_boss` | 보스 3종 × 기믹 5개 × 인원 1~4 = 60개 프로필의 성공·실패 경로, 패턴 4개씩, 스케줄러 규칙 | 420/420 |
| `tests/integration/run_integration.sh` | 2층 경로 원정 2인×2파티, 4인 + 5·6·7번째, 재접속, 재시작 | 42/42 |
| 4인 봇 스모크 (`snapshot_max`) | 수호목수·사수·주술사·공학자 4인, 서버 metrics 의 스냅샷 최대 크기 | 526B (코덱 전 1552B, MTU 1392B 초과 경고 0) |
| `tests/integration/run_export_smoke.sh` | export 실행 파일 왕복 (팩 에셋 로드) | 통과 |
| 봇 스모크 | 4인 혼합(톱니·주술사·공학자·사수) 완주, 보스 연습 서버(`--boss=`)에서 두꺼비·뿌리왕 2인 봇 전투 (스크립트 오류 0, 봇은 기믹을 못 풀어 전멸) | 통과 |
| `tests/integration/run_party_matrix.sh` | 동일 직업 4인 ×5, 힐러 없는 혼합, 1인 ×5, 혼합 | 아래 표 |

미검증: 실제 사람 플레이 전부(단계 5). 기믹 60개 프로필은 스크립트가 "해결 가능"함을 확인했을 뿐 재미·시간은 미측정. 봇은 기믹을 풀지 못하므로 보스 완주는 `test_boss` 의 스크립트 수행으로만 확인했다.

### 파티 조합 봇 완주 (BAL-01, 2층 경로, 결과는 아래에 갱신)
2026-09-20, 코덱·봇 수정 후 재실행. 12조합 모두 봇 오류 0, 스크립트 오류 0, MTU 초과 경고 0 (4인 스냅샷 최대 466~516B, 1인 225~296B).

| 조합 | 인원 | 결과 | 런 결과(1=승) | 처치 | 다운 | 구조 | 전멸 | 마지막 방 초 |
|---|---|---|---|---|---|---|---|---|
| g4 | 4 | ok | 1 | 19 | 0 | 0 | 0 | 40 |
| h4 | 4 | ok | 1 | 16 | 0 | 0 | 0 | 35 |
| mix_all | 4 | ok | 1 | 18 | 0 | 0 | 0 | 39 |
| mix_nohealer | 4 | ok | 1 | 9 | 0 | 0 | 0 | 8 |
| p4 | 4 | ok | 1 | 8 | 0 | 0 | 0 | 8 |
| s4 | 4 | ok | 1 | 18 | 0 | 0 | 0 | 40 |
| solo_g | 1 | ok | 1 | 9 | 0 | 0 | 0 | 27 |
| solo_h | 1 | ok | 2 | 8 | 1 | 0 | 1 | 28 |
| solo_p | 1 | ok | 1 | 5 | 0 | 0 | 0 | 14 |
| solo_s | 1 | ok | 1 | 8 | 0 | 0 | 0 | 45 |
| solo_w | 1 | ok | 1 | 8 | 0 | 0 | 0 | 40 |
| w4 | 4 | ok | 1 | 18 | 0 | 0 | 0 | 42 |

- 런 결과 1 = 승리, 2 = 전멸. `solo_h`(수액주술사 1인)만 두 번째 방에서 전멸: 봇이 회복 구역 안에서 버티지 못한다(사람 플레이 기준으론 미검증). 나머지 11조합은 완주.
- 코덱 전 실행에서는 4인 조합 전부에 MTU 초과 경고(1404~1584B)가 있었고 `s4`(톱니전사 4인)는 봇 사거리 버그로 전멸, `mix_all` 은 첫 방에서 1회 시간 초과(재실행 2회 통과)했다. 세 항목 모두 위 재실행에서 재현되지 않음.

## 에셋팩 v4 연결 검증 (2026-09-21)
| 검사 | 내용 | 결과 |
|---|---|---|
| 임포트 | v4 131 ID + 오디오 17 → 510장 합성, 매니페스트 399항목: 최종 394 · 파생 5 · 임시 0 | SKIP/FAIL 0 |
| `--tool=check_assets` | 399항목 규격·프레임 | 문제 0 |
| `test_v4_pack_assets` | 직업 3종 8상태·적 9종·보스 2종 시트, idle 보존, 타일 인덱스 수, 소품 상태 수, VFX·거목 활성 루프·예고 경계, 아이콘·초상·NPC, 임시 0, 카드 content_rect, 오디오 17 디코딩·루프 | 16/16 (단위 총 552) |
| 데모 캡처 | 톱니전사 방(새 시트·갉아 돌진·물가), 두꺼비 보스방(습지 타일·두꺼비), 뿌리왕 보스방(뿌리댐 타일·뿌리왕), 마을(NPC·훈련장·작업실). 스킬 캡처(데모가 Q/E/R 을 누르고 서버 시전 이벤트 뒤에 촬영): 거목 활성 루프, 도토리 산탄, 벌목 열풍, 뿌리 결속, 급류 밸브 — `docs/screenshots/skill_*.png` | 스크립트 오류 0, 런타임 대체 0 — `docs/screenshots/{room_v4_sawtooth,boss_toad_v4,boss_rootking_v4,hub_v4}.png` |
| 회귀 | 보스 420 · 통합 42 · export 스모크(ogg 포함) | 통과 |

미확인(실기): 4방향 체형·발 위치, 탈피·뿌리 재생 실제 타이밍, 오디오 청취·4인 동시 효과음 음량. 팩의 미확인 항목 대조는 `assets/packs/beaver_assets_v4/qa_results.json`.

## 적 밀도 상향 검증 (2026-09-21)
섬멸방 예산 9→14·웨이브 4·디렉터 예산 90%, 동시 상한 1인 12 / 4인 27. 봇 측정: 1인 섬멸방 23~25 스폰(이전 약 10), 4인 43~46. 스냅샷 최대 291B(1인)·556B(4인). 표는 `docs/roguelike_design.md` 4절.

## 로그라이크 설계 적용 검증 (2026-09-21, v0.3.0)
| 검사 | 내용 | 결과 |
|---|---|---|
| `-- --tool=run_tests` | 단위 536: `test_roguelike_systems` 32개 — 서약 정리·열기·프로필 반영·체크포인트, 위험도 상승·상한, 디렉터 증원 뒤 방 종료, 정예 접두 5종 효과, 전투 제단 강제 정예, 희귀도 가중치·min_rarity·리롤, 제단 사건 4종, 휴식/숫돌, 보스 갑각·실패 벌칙, 페이싱 수치 | 536/536 |
| `-- --tool=test_boss` | 갑각 온전 시 피해 60% 반영 후 | 420/420 |
| `-- --tool=check_routes` | 사건 풀 6종으로 확장 | 문제 0 |
| `tests/integration/run_integration.sh` | | 42/42 |
| 데모 캡처 | 서약 8종 선택 UI·열기 표시, HUD 위험도, 증원 토스트 | 스크립트 오류 0 (`docs/screenshots/hub_pacts.png`) |
| `run_party_matrix.sh` | 밀도 상향 뒤 봇 완주 (아래 표) | 아래 |

2026-09-21, 밀도 상향(웨이브 3·예산 9·디렉터 증원) 뒤 봇 12조합, 2층 경로. 스크립트 오류 0, MTU 경고 0.

| 조합 | 인원 | 결과 | 런 결과(1=승) | 처치 | 다운 | 구조 | 전멸 | 마지막 방 초 |
|---|---|---|---|---|---|---|---|---|
| g4 | 4 | ok | 1 | 20 | 0 | 0 | 0 | 8 |
| h4 | 4 | ok | 1 | 24 | 0 | 0 | 0 | 11 |
| mix_all | 4 | ok | 1 | 41 | 0 | 0 | 0 | 47 |
| mix_nohealer | 4 | ok | 1 | 21 | 0 | 0 | 0 | 8 |
| p4 | 4 | ok | 1 | 21 | 0 | 0 | 0 | 8 |
| s4 | 4 | ok | 2 | 29 | 4 | 0 | 4 | 26 |
| solo_g | 1 | ok | 1 | 11 | 0 | 0 | 0 | 16 |
| solo_h | 1 | ok | 2 | 2 | 1 | 0 | 1 | 21 |
| solo_p | 1 | ok | 1 | 13 | 0 | 0 | 0 | 23 |
| solo_s | 1 | ok | 1 | 10 | 0 | 0 | 0 | 15 |
| solo_w | 1 | ok | 2 | 14 | 1 | 0 | 1 | 18 |
| w4 | 4 | ok | 1 | 21 | 0 | 0 | 0 | 8 |

- 런 결과 1 = 승리, 2 = 전멸. 9/12 완주. 전멸 3: `s4`(톱니전사 4인, 근접만·2번째 방에서 4명 다운), `solo_h`(수액주술사 1인), `solo_w`(물길공학자 1인). 봇은 회피·회복 구역·포탑을 쓰지 않으므로 사람 기준 난이도는 아니지만, **근접 4인과 1인 지원 직업이 밀도 상향에 가장 약하다**는 점은 실기 검증에서 먼저 볼 항목이다. 조정 손잡이는 `docs/roguelike_design.md` 4절.
- 4인 방 시간(마지막 방 8~47초)은 봇이 몰려 있어 짧게 나온다. 사람 기준 목표(1인 12~15분, 4인 15~25분)는 실기로 잰다.

## 에셋팩 v3 연결 검증 (2026-09-21)
Release `assets-raw-v3` 의 두 팩(A 361장, B~E 158장)을 임포트해 매니페스트를 392항목(최종 205)으로 늘렸다. 팩의 QA_REPORT 가 "개발 쪽에서 확인" 으로 남긴 항목을 여기서 수행했다.

| 검사 | 방법 | 결과 |
|---|---|---|
| 임포트 | `tools/import_asset_pack.gd --packs=<v1·v2·v3 폴더>` | 316장 합성, SKIP/FAIL 0 |
| 파일·규격·프레임 | `--tool=check_assets` 392항목 | 문제 0 |
| 요청 수량·재생 규칙 | 단위 테스트 `test_v3_pack_assets`: A 22시트 열 수·4방향·시전 접점(attack/Q/R 2, E 3), 빈 껍질 256 예외, 탈피 단발, VFX 12종 프레임·모드, 덫 segments, 거목 hold_last, 타일 9/2/4, 소품 11종, 아이콘·UI 17 최종, 체력바 fill_rect, 9-slice 12px, 카드 변형 수 | 15/15 (단위 총 504) |
| 런타임 | 데모 캡처(사수 방 2개·보상 카드, 수호목수 보스방): 로고·패널·버튼, 마을 소품(기억나무 1단계·작업실 0단계·모집판·좌판), 강둑 링·물, 갉기 나무·통나무·강돌 변형, 사수 이동·공격, 멧돼지·까마귀·달팽이 이동/피격, 체력바·보스바·아이콘 | 스크립트 오류 0, 런타임 대체 0 — `docs/screenshots/{login,hub,room,reward,boss}_v3.png` |
| 회귀 | lint 49 · 보스 420 · 통합 42 · export 스모크 | 전부 통과 |

작화 관찰(보정 요청은 `docs/asset_request_v4.md` 0절): 강둑 타일의 투명 여백(코드로 바탕을 깔아 해결), 바닥 흙 패치 반복이 눈에 띔, 물가 코너 없음, 카드 안쪽 여백이 좁음. 4방향 발 위치 흔들림·손 위치 차이는 데모 캡처 해상도에서는 확인되지 않았고 실기 4PC 검증 항목으로 넘긴다.

## 플레이 관찰 (사용자 실기, v2 연결 빌드, 2026-09-20)
- 1인 기준 보스 처치까지 약 5분. 목표 15~25분에 크게 못 미친다.
- 기믹이 단순하게 느껴짐, 방마다 적 수가 적음.
- 원인 후보(수치 기준): 방당 웨이브 2회·기준 예산 6(달팽이 6마리 상당), 경로 5층 중 전투 2~3방, 보스 체력 520(1인). 대응안은 `docs/playtest_notes.md`.

## 에셋팩 v1·v2 연결 검증 (2026-09-20)
Release `assets-raw-v1` 의 두 팩(160 + 720 프레임)을 `tools/import_asset_pack.gd` 로 합성해 218장 시트를 만들고 매니페스트를 288항목으로 늘렸다(final 107 · derived 112 · placeholder 64 · planned 5).

| 검사 | 방법 | 결과 |
|---|---|---|
| 경로·존재 | `--tool=check_assets`: 288항목 파일 존재·`final_path` 우선·크기 = 프레임×열/행 | 문제 0 |
| RGBA·투명 배경 | 임포트 도구가 프레임을 `Image.FORMAT_RGBA8` 로 읽고 합성. 팩 QA(`assets/packs/*/qa_results.json`) 는 알파 있음·모서리 투명 | 통과 |
| 프레임 수 | 팩 4프레임 → 열 4. 대기(2프레임 팩)는 파생 2번째 프레임을 붙여 열 2. 장치는 4프레임 단일 행. 단위 테스트가 IC-01~05 15장의 열=4 를 확인 | 통과 |
| 앵커 | 팩 발 앵커 (64,106)/128 → `[0.5, 0.828]`, 보스 (256,424)/512 → 같은 비율(256 축소 후에도 동일). 화면 캡처에서 그림자 링과 발 위치 일치 | 통과(화면) |
| 루프 | idle/walk 루프, attack/cast/hit/down/death/stagger/exposed/molt 는 1회 재생 후 마지막 프레임 유지. 장치 success/failure 는 0.6초 1회 재생 후 유지, activation 은 진행률로 프레임 선택(자동 순환 없음) | 코드·화면 |
| export 포함 | `tests/integration/run_export_smoke.sh`(프로젝트 밖 임시 폴더에서 Linux export 실행) | 아래 참고 |
| 상태 ↔ 시트 | 단위 테스트 `test_entity_anim_sheets`: 수호목수 Q/E/R 시전 → `cast_q/e/r`, 보스 패턴 4개 → 팩 시트 4개, 경직/약점, 적 3종 5동작, IC 장치 15장 | 47/47 |
| 실제 화면 | 데모 모드 캡처: 마을(수호목수 idle), 전투(수액 달팽이·검은 새 공격, 수호목수 4방향·공격), 보스 방(철턱 가재 idle → 직선 돌진 예고·집게 휘두르기, IC-02 역류 수문 장치, 보스 체력 바) | `docs/screenshots/{hub,room_combat,room_combat_2,boss,boss_charge,boss_ic02,boss_ic01_device,reward}.png` |

서버와 애니메이션의 분리: 서버는 `action_kind`(basic/q/e/r/…) 와 보스 `pattern` 이름·상태 번호만 스냅샷에 실어 보내고 어떤 시트도 알지 못한다. 클라이언트가 `EntityView._pick_anim` 에서 상태 → 시트 ID 로 바꾸고, 시트가 없으면 가까운 동작(cast_q→cast→idle)으로 대체한다. 1인·4인·재접속 모두 같은 규칙이며, 재접속 시 스냅샷의 상태만으로 애니메이션을 복원한다(중간 이벤트에 의존하지 않는다; 장치 성공/실패 연출만 이벤트 기반이라 재접속 직후에는 activation 마지막 프레임으로 보인다).

## 단계 2 자동 검증 (모두 통과, 2026-09-20)
| 명령 | 내용 | 결과 |
|---|---|---|
| `-- --tool=run_tests` | 단위 187개(에셋 연결 47 포함): 기존 83 + 투사체·표식·돌진병·원거리병·회피 무적, 장치/거점/수문/건설/갉기/덫 목표, 경로 재현(GEN-01)·보상 3지선다·투표 동률·이탈자 기본 선택·안전 지점 합류·N 재산정, 상점 트랜잭션(ECO-01)·사건 다수결·체크포인트 왕복·재접속 재전송, 마을 보너스 상한(GROW-01/02) | 187/187 |
| `-- --tool=test_boss` | 철턱 가재: 패턴 4개 예고·판정, 경직 게이지·저항, 단계 전환 정리, 사망 정리, 기믹 5개 × 인원 1~4 = **20개 프로필**의 성공·실패 경로를 스크립트로 수행(MEC-01, BAL-02 데이터 검사), 스케줄러 규칙(미체험 우선·연속 금지·양립 불가·반복 상한) | 164/164 |
| `tests/integration/run_integration.sh` | 2방으로 축약한 던전(보상·문 이동 포함)을 2인×2파티, 4인 파티 + 5·6·7번째 클라이언트, 재접속, 서버 재시작 | 42/42 (2026-09-21 던전 격자 뒤 재실행) |
| 화면 캡처(Xvfb) | 장치 방·검은 새·보상 패널·던전 탐색(문·격자 지도)·다음 방 입장·보스 돌진 예고·보스 체력 바·마을 메뉴(내실·장비·숙련·도감·퀘스트 탭, 2026-09-21 개편). 장비 탭은 창고 항목 클릭 → EQUIP → ACCOUNT_UPDATE 왕복 뒤 장착 표시, 강화 버튼 → GEAR_ACTION → +1 표시·결정 차감·결과 VFX 까지 캡처. v5 팩 연결 뒤 아이콘·등급 테두리·배지·도안 자물쇠 표시 확인 | 확인 |

미검증·관찰: 봇 기준 4인 2방 원정은 2~4분으로 목표(15~25분)보다 짧다. 솔방울사수 봇은 거점 방에서 자주 전멸했다(봇의 조작 한계와 밸런스가 섞여 있어 실제 플레이로 분리 필요). 보스전 완주는 봇으로 확인하지 못했고(기믹 판단 불가), 기믹은 스크립트 검증만 했다. 20개 프로필 수행 가능성은 데이터 검사 수준이며 재미·시간은 미측정.

## 단계 1 자동 검증 (모두 통과, 2026-09-20)
| 명령 | 내용 | 결과 |
|---|---|---|
| `-- --tool=lint_all` | 33개 스크립트 로드·컴파일 | 0 실패 |
| `-- --tool=check_assets` | 매니페스트 83항목: 파일 존재·규격·프레임·참조 (AST-01 자동 항목) | 문제 0 |
| `-- --tool=run_tests` | 단위 83개: PBKDF2 표준 벡터, 계정 생성/로그인/토큰/잠금, 원자적 저장·.bak 복구, 판정 수학, 전투방(입력 중복, 이동, 회피, 공격 판정, 승리, 다운/구조/전멸, 전방 방어, 보호막), 인원 프로필 고정, 원정 정원 | 83/83 |
| `tests/integration/run_integration.sh` | 서버 1 + headless 클라이언트 봇 최대 10개 동시 | 42/42 |
| export 실행 파일 왕복 | `tests/integration/run_export_smoke.sh`: Linux 서버·클라이언트 export 를 프로젝트 밖 임시 폴더에 복사해 실행, 가입→원정→결과 왕복과 export 본 에셋 8종·폰트·효과음 로드 확인. CI 빌드 잡에서도 실행 | 통과 |
| Windows export | `TailExpedition.exe`(101MB, pck 내장), `tail-expedition-server.exe` 생성 | 생성만 확인, 실행 미검증 |
| 정상 종료 | `scripts/stop_server.sh` → STOP 파일 → 접속자 알림·저장 플러시·status `stopped=true` 후 종료 | 통과 |

통합 테스트가 확인한 항목:
| ID | 확인 내용 | 상태 |
|---|---|---|
| NET-01 | 4클라이언트가 같은 원정 시드·N=4 를 봄. 5번째는 마을 접속 후 `PARTY_FULL`, 6번째도 마을 이용 가능, 정원(6) 초과 7번째는 `SERVER_FULL` | 통과 |
| NET-03 | 전투 중 연결을 끊은 클라이언트가 재로그인 후 같은 원정 슬롯으로 복귀(파티 4명 유지) | 통과(같은 PC) |
| NET-04 | 모든 클라이언트 종료 후 새 로그인 성공, 서버 재시작 후 같은 `world_id` 와 `boot_count+1` | 통과 |
| HUB-01 | 마을에 선 채로 원정 생성 → 파티 나가기 → 다시 생성 (BAD_STATE 회귀, 봇 `hub_only --recreate`) | 통과 |
| NET-05 | 동시 원정 2개의 시드가 다르고 각 결과가 자기 파티 처치 수만 셈 | 통과 |
| AUTH-01 | 잘못된 비밀번호 `BAD_CREDENTIALS`, 중복 로그인 `ALREADY_ONLINE`, 토큰 재사용 거부(단위) | 통과 |
| CLIENT-01(일부) | 프로토콜 버전 불일치 시 게임 상태 전 `VERSION_MISMATCH` 와 요구 버전 안내. Linux export 실행 파일로 서버·클라이언트 왕복 확인, export 본에서 에셋 8종·폰트·효과음 로드 | 통과 / **Windows 실기 미검증** |
| RUN-01 | 가입→마을→원정 생성/참가→준비→출정→전투→결과→마을 복귀 | 통과(봇) |
| RUN-02(일부) | 다운 중 전멸 즉시 판정, 다운 타이머 만료 사망 | 단위 통과 |
| NET-02 | 클라이언트가 피해·체력·재화를 확정하는 RPC 자체가 없음(`c_msg`, `c_input` 만 존재). 서버 전용 `s_*` 는 authority 모드 | 구조상 보장, 악의적 클라이언트 실측 미수행 |
| SAVE-02 | 재시작 후 계정 통계(처치·클리어)와 불변 계정 ID 유지, 마을 누적 카운터 유지 | 통과 |
| GEN-02 | 방 지형 랜덤화: 18방 × 100시드 재현성·보호 지점·연결성 (`--tool=check_rooms`, 단위 test_room_gen) | 통과 |
| DROP-02 | 드랍 표 시뮬레이션 1,000판: 판당 19.4개, 전설 3.2판당 1개 (`--tool=drop_sim`) | 통과 |
| DROP-01 | 적 처치 → 바닥 장비 생성 이벤트 → 밟아 줍기(서버 창고 반영) → 버리기(본인 잠금·다음 틱 이벤트) · 스킬이 자동 공격보다 우선 (단위 test_ground_drops) | 통과 |
| ECO-01, GEN-01, BAL-*, MEC-*, TIME-01, GROW-*, PERF-01 | 단계 2 이후 콘텐츠 | 미구현/미검증 |

## 발견·수정한 결함
- **export 빌드에서 모든 에셋이 대체 도형으로 표시됨** (Windows 실기에서 발견). 원인: pck 에는 원본 png/ttf/wav 가 아니라 import 된 리소스만 들어가는데 AssetRegistry 가 원본 파일 존재만 확인했다. 초기 Linux 검증은 프로젝트 폴더에서 실행해 원본을 읽는 바람에 통과했다. 수정: `ResourceLoader.exists` 로도 해석하고, export 스모크 테스트를 프로젝트 밖 폴더에서 실행하도록 바꿨다.

## 화면 검증 (Xvfb + Mesa llvmpipe, OpenGL3 호환 렌더러)
- 클라이언트 데모 모드(`--demo=<nick> --shots=<dir>`)로 실제 클라이언트를 가상 디스플레이에서 실행해 로그인·마을·파티·전투·결과 화면을 캡처했다. `docs/screenshots/` 참고.
- 확인한 것: 한국어 폰트 렌더링, 임시 비버/달팽이 스프라이트와 4방향·공격 애니메이션, 적 강공격 예고(주황 링+채워지는 원), HUD(체력·회피 충전·Q/E/R 아이콘·회복 횟수·파티 상태·웨이브), 결과 화면과 보상, 개발 화면(F3).
- 확인하지 못한 것: 실제 키보드·마우스 **조작감**, 60fps 성능(소프트웨어 렌더러 기준 70~140fps 는 참고값일 뿐), Windows 실기 실행, 인터넷 지연 환경.
- Windows 클라이언트 export 는 preset 만 준비했다. CI(GitHub Actions) 가 `TailExpedition.exe` 를 만들며, 실제 Windows 실행은 사용자가 확인해야 한다.

## 성능 메모 (측정값, 보장 아님)
- 서버 틱: 4인 원정 1개 + 마을 2명 기준 평균 0.1ms 미만, 최대 수 ms (컨테이너, 봇 클라이언트). `server_status.json` 과 로그 metrics 줄에 기록된다.
- 전투 스냅샷(4인, 적 ~7): 패킹 후 ENet MTU(1392B) 안에 들어온다. 통합 테스트 서버 로그에 MTU 경고 0회. 
